# CLEAR-LeWM results

The canonical result set is [v0.5](v0.5/README.md). It contains the matched
seed-42 CEM matrix for official, project-trained, and random policies plus the
project-trained GD solver ablation. Each headline number is backed by a
structured `results.txt.json` under `v0.5/raw/`.
