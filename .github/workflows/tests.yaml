name: stable-worldmodel
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - uses: pre-commit/action@v3.0.1

  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: False
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    env:
      MUJOCO_GL: osmesa
      PYOPENGL_PLATFORM: osmesa
    steps:
    - uses: actions/checkout@v5
    - name: System deps for MuJoCo/OpenGL/FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libgl1-mesa-dev libgl1 libglx-mesa0 libglfw3 \
          libosmesa6-dev libegl1 libopengl0 patchelf ffmpeg
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        python-version: ${{ matrix.python-version }}
    - name: Dependencies
      run: uv sync --all-extras
    - name: Tests
      run: uv run --group dev pytest
    - name: Build
      run: uv build
    - name: PyPi
      run: uv pip install stable-worldmodel


  docs:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
    env:
      MUJOCO_GL: osmesa
      PYOPENGL_PLATFORM: osmesa
    steps:
    - uses: actions/checkout@v5
    - name: System deps for MuJoCo/OpenGL/FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libgl1-mesa-dev libgl1 libglx-mesa0 libglfw3 \
          libosmesa6-dev libegl1 libopengl0 patchelf ffmpeg
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        python-version: "3.12"
    - name: Dependencies
      run: |
        uv sync --group dev
        uv pip install -e .
    - name: Build docs
      run: uv run --group dev mkdocs build
    - uses: actions/configure-pages@v5
    - uses: actions/upload-pages-artifact@v3
      with:
        path: site
    - uses: actions/deploy-pages@v4
